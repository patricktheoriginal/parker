# 3D route map (embedded in Parker) dependencies.
# Install with:  pip install -r requirements-3dmap.txt
#
# Without this, the 3D route map still works but opens in your default web
# browser instead of inside the Parker window.
#
# Routes come from OSRM (free, open-source, no API key). No Google key needed.

PyQt6-WebEngine
